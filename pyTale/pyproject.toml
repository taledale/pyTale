[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pytale"
version = "0.5.4-1"
description = "pyTale Python library for Hytale plugins"
requires-python = ">=3.12,<3.13"

[tool.hatch.build.targets.wheel]
packages = ["pytale"]
