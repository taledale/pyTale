[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-plugin"
version = "1.0.0"
description = "Test plugin for pyTale"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["test_plugin"]
